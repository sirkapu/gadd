# CLAUDE.md — templates/

- Templates carry PLACEHOLDERS, never a deployment's real paths or names
  (residue rules; see [bin/residue-check.sh](../bin/residue-check.sh)).
- A template that "protects" nothing must say so explicitly (commented
  example) rather than ship as an inert-but-active rule.

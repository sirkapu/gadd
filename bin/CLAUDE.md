# CLAUDE.md — bin/

- Operational scripts, zero-dependency Node or bash. The measurement
  instruments live here (North Star fleet aggregator; parity engine under
  adapters/lv/checks/lib/); an instrument is never modified without its
  acceptance corpus in [tests/](../tests/) passing.
- Substrate rule (ledger): substrate classes get reimplemented, not waived.
